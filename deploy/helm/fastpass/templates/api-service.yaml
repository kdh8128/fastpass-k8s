apiVersion: v1
kind: Service
metadata:
  name: fastpass-api
  namespace: {{ .Values.namespace }}
  labels:
    app: fastpass-api
spec:
  type: ClusterIP
  selector:
    app: fastpass-api
  ports:
    - name: http
      port: {{ .Values.api.port }}
      targetPort: {{ .Values.api.port }}