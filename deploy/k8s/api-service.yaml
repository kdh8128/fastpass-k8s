apiVersion: v1
kind: Service
metadata:
  name: fastpass-api
  namespace: fastpass
spec:
  type: ClusterIP
  selector:
    app: fastpass-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080